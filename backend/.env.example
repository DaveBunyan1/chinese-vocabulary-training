# App Settings
ENVIRONMENT=development
PORT=8000
LOG_LEVEL=INFO

# Local DB Connection (for running backend locally outside Docker)
DATABASE_URL=postgresql+asyncpg://dev_user:secret_password@localhost:5432/chinese_learning_dev